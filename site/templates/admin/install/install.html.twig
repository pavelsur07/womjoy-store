<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {% block stylesheets %}
        {{ encore_entry_link_tags('admin') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('admin') }}
    {% endblock %}
</head>
<body>
    <div class="container-xxl">
        <section class="section mt-4">
            <h1 class="mb-4 text-center text-uppercase">ИНСТАЛЯЦИЯ НОВОГО САЙТА</h1>

            {{ form_start(form) }}

            <div class="row justify-content-center">
                {% if error %}
                    <div class="col col-12 col-lg-10 mb-4">
                        <div class="p-3 bg-danger bg-opacity-10 text-danger">{{ error }}</div>
                    </div>
                {% endif %}
                <div class="col col-12 col-lg-5">
                    <div class="w-field mb-4">
                        <label class="w-field__label">ИМЯ *</label>
                        <div class="w-field__main">
                            <input
                                    name="{{ field_name(form.firstName) }}"
                                    value="{{ field_value(form.firstName) }}"
                                    placeholder="Наталья"
                                    class="w-field__inp"
                            >
                        </div>
                    </div>
                </div>

                <div class="col col-12 col-lg-5">
                    <div class="w-field mb-4">
                        <label class="w-field__label">МОБИЛЬНЫЙ ТЕЛЕФОН *</label>
                        <div class="w-field__main">
                            <input type="tel"
                                   name="{{ field_name(form.phone) }}"
                                   value="{{ field_value(form.phone) }}"
                                   placeholder="+7 (999) 999-99-99"
                                   class="w-field__inp"
                            >
                        </div>
                    </div>
                </div>

                <div class="col col-12 col-lg-5">
                    <div class="w-field mb-4">
                        <label class="w-field__label">ЭЛЕКТРОННАЯ ПОЧТА *</label>
                        <div class="w-field__main">
                            <input type="email"
                                   name="{{ field_name(form.email) }}"
                                   value="{{ field_value(form.email) }}"
                                   placeholder="email@site.ru"
                                   class="w-field__inp"
                            >
                        </div>
                    </div>
                </div>

                <div class="col col-12 col-lg-5">
                    <div class="w-field mb-4 mb-lg-0">
                        <label class="w-field__label">ПАРОЛЬ *</label>
                        <div class="w-field__main">
                            <input
                                    name="{{ field_name(form.password) }}"
                                    value="{{ field_value(form.password) }}"
                                    placeholder="********"
                                    class="w-field__inp"
                            >
                        </div>
                        <span class="w-field__descr">Минимум 8 символов</span>
                    </div>
                </div>

                <div class="col col-12 col-lg-10">
                    <label class="w-check mt-4">
                        <input type="checkbox" hidden="">
                        <span class="w-check__sq"><img src="{{ asset('./pion/img/icons/white-check.svg') }}" alt="" width="18" height="13"></span>
                        <div class="w-check__main">
                            <span class="w-check__name">Подтверждаю свое согласие на получение информации о новинках и выгодных предложениях.</span>
                        </div>
                    </label>
                    <label class="w-check mt-4">
                        <input type="checkbox" hidden="">
                        <span class="w-check__sq"><img src="{{ asset('./pion/img/icons/white-check.svg') }}" alt="" width="18" height="13"></span>
                        <div class="w-check__main">
                            <span class="w-check__name">Подтверждаю свое согласие на обработку и хранение моих персональных данных в соответствии с пользовательским соглашением</span>
                        </div>
                    </label>
                    <label class="w-check mt-4">
                        <input type="checkbox" hidden="">
                        <span class="w-check__sq"><img src="{{ asset('./pion/img/icons/white-check.svg') }}" alt="white-check" width="18" height="13"></span>
                        <div class="w-check__main">
                            <span class="w-check__name">Соглашаюсь с условиями программы лояльности</span>
                        </div>
                    </label>

                    <button class="w-primary-btn w-100 mt-5" type="submit">Зарегистрироваться</button>
                </div>
            </div>
            {{ form_end(form) }}
        </section>
    </div>
</body>
</html>