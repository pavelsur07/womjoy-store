{% extends 'admin/base.html.twig' %}

{% block title %}Product index{% endblock %}

{% block body %}

    <div class="page-header d-print-none mb-3">
        <div class="container-xl">
            <div class="row g-2 align-items-center">
                <div class="col">
                    <!-- Page pre-title -->
                    <div class="page-pretitle">
                        Overview
                    </div>
                    <h2 class="page-title">
                        Dashboard
                    </h2>
                </div>
                <!-- Page title actions -->
                <div class="col-auto ms-auto d-print-none">
                    <div class="btn-list">

                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="col-12">
        <div class="card card-md">
            <div class="card-stamp card-stamp-lg">
                <div class="card-stamp-icon bg-primary">
                    <!-- Download SVG icon from http://tabler-icons.io/i/ghost -->
                    <svg xmlns="http://www.w3.org/2000/svg" class="icon" width="24" height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round"><path stroke="none" d="M0 0h24v24H0z" fill="none"></path><path d="M5 11a7 7 0 0 1 14 0v7a1.78 1.78 0 0 1 -3.1 1.4a1.65 1.65 0 0 0 -2.6 0a1.65 1.65 0 0 1 -2.6 0a1.65 1.65 0 0 0 -2.6 0a1.78 1.78 0 0 1 -3.1 -1.4v-7"></path><path d="M10 10l.01 0"></path><path d="M14 10l.01 0"></path><path d="M10 14a3.5 3.5 0 0 0 4 0"></path></svg>
                </div>
            </div>
            <div class="card-body">
                <div class="row align-items-center">
                    <div class="col-10">
                        <h3 class="h1">Test function</h3>
                        <div class="mt-3">
                            <a href="{{ path('admin.dashboard.wb') }}" class="btn btn-bitbucket mb-1">WB</a>
                            <a href="{{ path('admin.dashboard.generate.yml') }}" class="btn btn-bitbucket mb-1">Generate YML</a>
                            <a href="{{ path('admin.dashboard.generate.sitemap') }}" class="btn btn-bitbucket mb-1">Generate Sitemap</a>
                            <a href="{{ path('admin.dashboard.send_email') }}" class="btn btn-bitbucket mb-1">Send Email</a>
                            <a href="{{ path('admin.dashboard.category.update.filter') }}" class="btn btn-bitbucket mb-1">Update filter all category</a>
                            <a href="{{ path('admin.dashboard.cart_clear') }}" class="btn btn-bitbucket mb-1">Cart clear</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
