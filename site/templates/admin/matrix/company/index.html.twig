{% extends 'admin/base.html.twig' %}

{% block title %}Company index{% endblock %}

{% block body %}
    <div class="row g-2 align-items-center">
        <div class="col">
            <!-- Page pre-title -->
            <div class="page-pretitle">
                Matrix
            </div>
            <h2 class="page-title">
                Company index
            </h2>
        </div>
        <!-- Page title actions -->
        <div class="col-auto ms-auto d-print-none">
            <div class="btn-list">
            </div>
        </div>
    </div>

    {#<div class="mt-3 mb-3">
        <a class="btn btn-bitbucket" href="{{ path('store.admin.attribute.new') }}">Create new</a>
    </div>#}
    <div class="card card-body mt-2">
       {# <table class="table">
            <thead>
            <tr>
                <th class="w-1">Data</th>
                <th>Service</th>
                <th>Product</th>
                <th>Sale</th>
                <th>Cost</th>
                <th>Action</th>
            </tr>
            </thead>
            <tbody>
            {% for sale in pagination %}
                <tr>
                    <td>{{ sale.createdAt | date('d-m-Y') }}</td>
                    <td>{{ sale.service }}</td>
                    <td>{{ sale.productIdentity }}</td>
                    <td>{{ sale.sale }}</td>
                    <td>{{ sale.cost }}</td>
                    <td><a href="#">edit</a></td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="8">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>#}


        <div class="pagination mt-3">
            {# {{ knp_pagination_render(pagination) }}#}
        </div>
    </div>
{% endblock %}
